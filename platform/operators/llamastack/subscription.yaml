# Default on the *spoke* cluster (no RHOAI / no MaaS). Do not install this
# together with platform/operators-with-rhoai (DSC-managed operator).
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: llama-stack-k8s-operator
  namespace: llama-stack-system
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  channel: alpha
  installPlanApproval: Automatic
  name: llama-stack-k8s-operator
  source: community-operators
  sourceNamespace: openshift-marketplace
